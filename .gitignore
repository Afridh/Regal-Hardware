node_modules/
dist/
.env
*.log
.DS_Store
.pgdata/
# day sheets published by the Shift Board at run time
app/reports/
.neon
.env.local
.claude/

# the print helper's own key on that PC
tools/print-agent/config.local.json

# database dumps: the whole shop in the clear — customers, mobiles, what everyone owes.
# They are a safety net kept on this machine, never something to push anywhere.
server/db/backups/
